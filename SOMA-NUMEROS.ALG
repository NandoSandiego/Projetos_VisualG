algoritmo "semnome"

//Apertar: ctrl + space
var

num1,num2, res : real

inicio
   escreva("Digite o 1º número: ")
   leia(num1)
   escreva("Digite o 2º número: ")
   leia(num2)
   escreval("*-*-*-*-*-*-*-*-*-*")
   res <- num1 + num2
   escreval("O valor entre os números é: ",res)

fimalgoritmo